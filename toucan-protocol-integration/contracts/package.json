{
  "name": "hardhat-project",
  "scripts": {
    "dashboard": "npx truffle dashboard",
    "deploy": "npx hardhat run ./scripts/deploy.ts --network dashboard"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@nomicfoundation/hardhat-verify": "^1.0.0",
    "hardhat": "^2.14.0"
  }
}
